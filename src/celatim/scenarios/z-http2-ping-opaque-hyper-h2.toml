schema_version = "celatim.scenario.v1"
scenario_id = "http2-ping-opaque-hyper-h2"
mechanism_id = "http2-ping-opaque"
description = "Non-privileged hyper-h2 client/server HTTP/2 PING opaque-bytes stack scenario."
evidence_tier = "real_daemon_path"
privilege = "none"
expected_runtime_s = 5.0
requires_tools = []
requires_extras = ["daemon"]
payload_hex = "00 ff 80 41 42 43"
control_message = "control"

[transport]
kind = "http2_hyper_h2"
transcript_json = "out/transcripts/{scenario_id}-{case}.json"
validate_ack = true

[pacing]
unit_rate_hz = 20.0
timing_quantum_s = 0.005
