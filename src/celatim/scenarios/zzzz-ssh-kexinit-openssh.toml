schema_version = "celatim.scenario.v1"
scenario_id = "ssh-kexinit-openssh-real-daemon"
mechanism_id = "ssh-kexinit-cookie"
description = "Paramiko client completing KEXINIT with a production OpenSSH daemon."
evidence_tier = "real_daemon_path"
privilege = "none"
expected_runtime_s = 15.0
requires_tools = ["sshd"]
requires_extras = ["ssh"]
payload_hex = "00 ff 80 51 55 49 43"
control_message = "control"

[transport]
kind = "ssh_kexinit_openssh"
dst_ip = "127.0.0.1"
dst_port = 22
timeout_s = 10.0
transcript_json = "../artifacts/ssh-kexinit-openssh/{scenario_id}-{case}.json"
