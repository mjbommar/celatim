schema_version = "celatim.scenario.v1"
scenario_id = "websocket-websockets"
mechanism_id = "websocket-tunnel"
description = "Non-privileged paired real-codec client/server scenario for websocket-tunnel."
evidence_tier = "real_daemon_path"
privilege = "none"
expected_runtime_s = 5.0
requires_tools = []
requires_extras = ["realtime"]
payload_hex = "00 ff 80 51 55 49 43"
control_message = "control"

[transport]
kind = "websocket_websockets"
